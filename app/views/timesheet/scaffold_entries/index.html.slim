.page-header
  = link_to new_timesheet_scaffold_entry_path, class: 'btn btn-primary' do 
    span.glyphicon.glyphicon-plus
    |  New Scaffold entry
  h1
    | Listing timesheet_scaffold_entries

.table-responsive
  table.table.table-striped.table-bordered.table-hover
    thead
      tr
        th
          | Timesheet
        th
          | Equipment
        th
          | Fuel gallons
        th
          | Mileage start
        th
          | Mileage end
        th
        th
        th
    tbody
      - @timesheet_scaffold_entries.each do | timesheet_scaffold_entry |
        tr
          td
            = timesheet_scaffold_entry.timesheet
          td
            = timesheet_scaffold_entry.equipment
          td
            = timesheet_scaffold_entry.fuel_gallons
          td
            = timesheet_scaffold_entry.mileage_start
          td
            = timesheet_scaffold_entry.mileage_end
          td
            = link_to 'Show', timesheet_scaffold_entry
          td
            = link_to 'Edit', edit_timesheet_scaffold_entry_path(timesheet_scaffold_entry) 
          td
            = link_to 'Destroy', timesheet_scaffold_entry, method: :delete, data: { confirm: 'Are you sure?' } 
