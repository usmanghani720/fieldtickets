.page-header
  = link_to new_admin_customer_path, class: 'btn btn-primary' do 
    span.glyphicon.glyphicon-plus
    |  New Customer
  h1
    | Customers

.table-responsive
  table.table.table-striped.table-hover
    thead
      tr
        th
          | Name
    tbody
      - @customers.each do | customer |
        tr data-link="#{admin_customer_path(customer)}"
          td
            = customer.name