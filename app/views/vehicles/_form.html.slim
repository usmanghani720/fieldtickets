= form_for(@vehicle, html: { class: "form-horizontal", role: "form" }) do |f|
  - if @vehicle.errors.any?
    .alert.alert-danger.alert-dismissable role="alert"
      button.close type="button" data-dismiss="alert"
        span aria-hidden="true"
          | &times;
        span.sr-only
          | Close
      h4= "#{pluralize(@vehicle.errors.count,"error")} prohibited this vehicle from being saved:"
      ul
        - @vehicle.errors.full_messages.each do |msg|
          li= msg
  .form-group
    = f.label :internal_number, class: "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :internal_number, class: "form-control"
  .form-group
    = f.label :description, class: "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :description, class: "form-control"
  .form-group
    = f.label :vehicle_type, class: "col-sm-2 control-label"
    .col-sm-10
      = f.select :vehicle_type, options_for_select(Vehicle::TYPES), {}, {class: 'selectpicker'}
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: "btn btn-primary"