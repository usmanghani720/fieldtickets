= form_for(@equipment, html: { class: "form-horizontal", role: "form" }) do |f|
  - if @equipment.errors.any?
    .alert.alert-danger.alert-dismissable role="alert"
      button.close type="button" data-dismiss="alert"
        span aria-hidden="true"
          | &times;
        span.sr-only
          | Close
      h4= "#{pluralize(@equipment.errors.count,"error")} prohibited this equipment from being saved:"
      ul
        - @equipment.errors.full_messages.each do |msg|
          li= msg
  .form-group
    = f.label :internal_number, class: "col-sm-2 control-label"
    .col-sm-10
      = f.text_area :internal_number, class: "form-control"
  .form-group
    = f.label :license_plate_number, class: "col-sm-2 control-label"
    .col-sm-10
      = f.text_area :license_plate_number, class: "form-control"
  .form-group
    = f.label :vin, class: "col-sm-2 control-label"
    .col-sm-10
      = f.text_area :vin, class: "form-control"
  .form-group
    = f.label :vehicle_type, class: "col-sm-2 control-label"
    .col-sm-10
      = f.text_area :vehicle_type, class: "form-control"
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: "btn btn-primary"
